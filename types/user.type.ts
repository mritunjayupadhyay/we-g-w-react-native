export type User = {
    id: number,
    name: string
}

export type UserCard = {
    expiration_year: number,
    expiration_month: number,
    name: string,
    last_digits: string,
    brand: string,
    bank: string
}

